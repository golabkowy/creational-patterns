package factoryMethod;

// REMEMBER!
// Interface methods are public-abstract by default (implicit) and it is not possible to use other specifier
public interface Product {
    void someMethod();
    int someIntMethod();
}
